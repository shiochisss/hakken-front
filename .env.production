# 本番ビルド時に読まれる（NEXT_PUBLIC_ 付きの値はJSへ焼き込まれる）。
#
# ★ NEXT_PUBLIC_API_BASE_URL は廃止した（BFF・API設計書 v1.8 A-12）。
#   ブラウザは相対パスしか叩かず、実際の転送先はサーバ専用の環境変数 API_ORIGIN で決まる。
#   API_ORIGIN は **Azure Static Web Apps のアプリ設定**で与えること。ここには書かない
#   （ここに書くとリポジトリにURLが残り、隠す意味が薄れる）。
NEXT_PUBLIC_MOCK=0
